const somar = (a,b) => { //nesse caso foi usado o bigodinho porque precisamos usar uma segunda linha
    console.log(a+b);
}
somar(10,10)

const dividir = (a,b) => console.log('a minha divisão é '+ a/b); // neste caso não usamos bigodinho porque ele está tudo dentro da mesma linha
dividir(10,2)
